PORT=4173
PM2_NAME=lo-even

# Who to write to about a forgotten password. The same variable lo reads, so the
# sign-in screen here and lo's own name the same person; left empty, the sheet
# says there is no address set rather than offering a letter nobody receives.
VITE_ADMIN_EMAIL=
